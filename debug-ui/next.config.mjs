/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // this triggers a warning? removed for now... no idea what it does...
    // appDir: false,
  },
};

export default nextConfig;
